module.exports = conf => {
    const config = {};
    config.AppID=''; // 填写您自己的appid
    config.API_Key=''; //填写您自己的api_key
    config.Secret_Key=''; // 填写您自己的 secret_key
    return config;
};

// 配置申请地址 https://ai.baidu.com/